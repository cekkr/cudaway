#pragma once

#include <cstdint>

namespace cudaway::types {

using ModuleHandle = std::uint64_t;
using FunctionHandle = std::uint64_t;
using ContextHandle = std::uint64_t;

}  // namespace cudaway::types
